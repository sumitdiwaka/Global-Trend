{
  "name": "task-management-frontend",
  "version": "1.0.0",
  "description": "Frontend for Task Management App",
  "scripts": {
    "start": "live-server src --port=3000 --host=localhost --open=/pages/index.html",
    "dev": "live-server src --port=3000 --host=localhost"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}